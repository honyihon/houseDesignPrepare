{
  "name": "house-design-prepare-browser-qa",
  "private": true,
  "version": "0.1.0",
  "description": "Browser regression tests for the offline house design viewers.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}
